<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <title>Quotes Index</title>
    <link rel="stylesheet" href="style.css">
</head>

<body>
        <h1>Welcome to Quoting Dojo</h1>
        
        <% if(messages) { %>
            <ul>
            <% for (var x in messages.add_quote) { %>
                <li><%= messages.add_quote[x] %></li>
            <% } %>
            </ul>
        <% } %>
        <form action="/add_quote" method="post">
            <table>
                <tbody>
                    <tr>
                        <td>Name: </td>
                        <td><input type="text" name="name" id="name"></td>
                    </tr>
                    <tr>
                        <td>Quote: </td>
                        <td><input type="text" name="quote" id="quote"></td>
                    </tr>
                </tbody>
            </table>
            <input type="submit" value="Add my quote">
        </form>
        <a href="/quotes"><button>Skip to quotes</button></a>
    </body>


</html>